# Checkpoint — vpn-tray — 2026-07-05 21:51

## Goal
Tray widget VPN Mullvad (Tailscale exit-node) su GNOME: spostato nella cartella progetti, risolto bug submenu, riscritto selettore nodi come popup HTML WebKit con bandiere/tendine/ricerca, aggiunte feature Priority-based.

## Current State (funzionante, verificato)
- Servizio `vpn-tray.service` **active** (systemd --user), watchdog + Restart=always
- Script: `/mnt/backup/Dropbox/1 Programmazione/Progetti/vpn-tray/vpn-tray.py` (~18KB)
- Menu tray: Status · Disattiva · 🎲 Nodo casuale · ⚡ Random veloce (top nodi) · 🌍 Scegli nodo… · 🔁 Rotazione automatica · Aggiorna · Esci
- Popup "Scegli nodo…" = finestra WebKit2 con paesi a tendina (`<details>`), bandiere emoji, città ordinate per Priority (badge ★), ricerca live, click→connetti

## Progress / TODO Board
### ✅ Done (verificato)
- [x] Spostato progetto `~/.local/share/vpn-tray` → `/mnt/backup/Dropbox/1 Programmazione/Progetti/vpn-tray` · systemd aggiornato (ExecStart con path tra virgolette per lo spazio)
- [x] Root-cause bug submenu: VOLUME/nesting DBusMenu (test empirico) — ubuntu-appindicators regge solo 1 livello submenu foglia
- [x] Selettore nodi riscritto come popup HTML WebKit2 4.1 · syntax OK · servizio active
- [x] Bandiere emoji per paese (`_flag` da CountryCode ISO-2)
- [x] Nodi ordinati per Priority Mullvad (badge ★) nel popup
- [x] ⚡ Random veloce (`on_random_fast`): top 10% Priority
- [x] Rotazione automatica ristretta ai top 10% Priority (`on_rotate`, rispetta country_lock)
- [x] Vault sessione aggiornato + committato (53 file via lock)

### ⬜ To-Do
- [ ] **VERIFICA-UTENTE** — confermare live che il popup "Scegli nodo…" si apra, le tendine paese/città funzionino, la ricerca filtri, e il click connetta
      Comando: click su icona tray → "🌍 Scegli nodo…"
      Done when: popup si apre, bandiere visibili, click su città connette + status tray aggiornato con ✓
- [ ] **(opzionale) git init** — il progetto NON è repo git. Se vuoi versionarlo: `cd "/mnt/backup/Dropbox/1 Programmazione/Progetti/vpn-tray" && git init && git add vpn-tray.py .claude && git commit -m "feat: vpn tray webkit node picker"`
      Done when: `git log` mostra il commit iniziale

## Failed Approaches (NON riprovare)
- ❌ Submenu Gtk paese→città (3 livelli): ubuntu-appindicators non renderizza oltre 2 livelli
- ❌ Appiattire 50 paesi nel menu principale (2 livelli): rompe per VOLUME (500+ item DBusMenu)
- ❌ Lazy rebuild submenu su evento `select`: non ripopola in tempo per DBusMenu
- ❌ `city_menu.show_all()` su menu vuoto prima di append: serializza DBus con 0 figli
- ❌ `set_menu(None)` come reset cache: `AssertionError GTK_IS_MENU`
- ❌ `tailscale ping` sui nodi Mullvad per la velocità: timeout, non rispondono se non connessi

## Do NOT
- ❌ Non usare submenu Gtk per liste lunghe/annidate su ubuntu-appindicators → usa WebKit/finestra
- ❌ Non reintrodurre wrapper "Scegli paese" né i 50 paesi appiattiti nel menu tray
- ❌ Non disabilitare `vpn-tray.service` — è always-on by design (kill → rinasce in 3s)
- ❌ Non tentare ping per-nodo per la velocità (impossibile su Mullvad inattivi)
- ❌ Non cancellare `sensors.js.bak.20260626_2233` (progetto Vitals, altra sessione)

## Key Decisions
- Selettore nodi = popup WebKit HTML (non submenu tray) per aggirare limite DBusMenu
- "Velocità nodo" = Priority Mullvad (0-500), unico dato reale disponibile (no ping per-nodo)
- Random veloce + rotazione = top 10% Priority (min 10 nodi)

## Code Context (firme chiave, verbatim)
- `list_nodes()` → dict con `host, ip, country, country_code, city, priority`
- `current_node()` → dict con `country, country_code, city, ip, host`
- `_flag(country_code)` @staticmethod → emoji bandiera da ISO-2
- `on_choose_node(_)` → apre Gtk.Window con WebKit2.WebView, HTML da `_chooser_html()`
- `_on_web_connect(manager, result)` → riceve ip da JS, connette, chiude
- `on_random_fast(_)` / `on_rotate()` → `sorted(pool, key=-priority)[:max(10, len//10)]`

## Setup / Deps
- python3-gi, gir1.2-ayatanaappindicator3, gir1.2-webkit2-4.1 (libwebkit2gtk-4.1)
- Ambiente GNOME Shell 46.0, X11, ubuntu-appindicators@ubuntu.com

## Resume Instructions
Il lavoro è completo e funzionante. Resta solo la verifica live dell'utente sul popup. Se emergono problemi, lo script è in `/mnt/backup/Dropbox/1 Programmazione/Progetti/vpn-tray/vpn-tray.py`; dopo ogni modifica: `systemctl --user restart vpn-tray && systemctl --user is-active vpn-tray`. Per editare codice serve override hook gerarchia (`touch ~/.claude/state/.hierarchy_allow`).

## Warnings
- Il file NON è sotto git → nessun undo automatico. Backup manuale: `vpn-tray.py.bak.20260626_2128`
- Editare lo script richiede override enforce_hierarchy (progetto Dropbox = codice)
